services:
  frontend:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8083/api}
        VITE_GOOGLE_CLIENT_ID: ${VITE_GOOGLE_CLIENT_ID:?Falta VITE_GOOGLE_CLIENT_ID en frontend/.env}
    ports:
      - "5173:80"
    restart: unless-stopped
